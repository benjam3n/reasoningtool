---
import Base from '../layouts/Base.astro';

const tier1 = [
  { name: 'se', desc: 'Systematically lists all options in a problem space so nothing gets missed' },
  { name: 'aex', desc: 'Surfaces the hidden assumptions in any claim, plan, or argument' },
  { name: 'cmp', desc: 'Structured comparison of options with explicit criteria and trade-offs' },
  { name: 'ht', desc: 'Tests claims by examining what would be true if right vs. wrong' },
  { name: 'dcp', desc: 'Builds a step-by-step flowchart anyone can follow for a recurring decision' },
  { name: 'dcm', desc: 'Breaks complex problems into smaller, solvable parts' },
  { name: 'rca', desc: 'Finds the actual cause of a problem, not just symptoms' },
  { name: 'dd', desc: 'Identifies all the dimensions along which something can vary' },
  { name: 'mv', desc: 'Checks that a breakdown is mutually exclusive and collectively exhaustive' },
  { name: 'ins', desc: 'Combines findings from multiple analyses into coherent conclusions' },
  { name: 'cba', desc: 'Structured evaluation of costs vs. benefits with explicit trade-offs' },
  { name: 'gu', desc: 'Clarifies what someone actually wants vs. what they said they want' },
];

const tier2 = [
  { name: 'pv', desc: 'Checks whether a procedure works by testing each step' },
  { name: 'ai', desc: 'Flips assumptions to discover what happens if the opposite is true' },
  { name: 'to', desc: 'Finds the correct order when tasks depend on each other' },
  { name: 'cda', desc: 'Applies solutions from one field to problems in another' },
  { name: 'ma', desc: 'Generates combinations by varying parameters across dimensions' },
  { name: 'sya', desc: 'Maps how parts of a system interact and where leverage points are' },
  { name: 'br', desc: 'Works backward from the goal to figure out what steps are needed' },
  { name: 'prm', desc: 'Imagines the project failed and asks why, before it starts' },
  { name: 'fla', desc: 'Identifies likely failure modes before they happen' },
  { name: 'poa', desc: 'Explores what is possible, not just what is likely' },
  { name: 'ifss', desc: 'Searches the space of valid inferences from given premises' },
  { name: 'rci', desc: 'Keeps asking "why" until you reach the actual root' },
  { name: 'la', desc: 'Identifies real constraints and which ones can be changed' },
  { name: 'vbo', desc: 'Checks every claim for evidence before presenting it' },
  { name: 'val', desc: 'Uses multiple independent checks to validate a conclusion' },
];

const experimental = [
  { name: 'araw', desc: 'Assume Right / Assume Wrong — tests both branches of any claim recursively' },
  { name: 'uaua', desc: 'Universalize → ARAW → Universalize → ARAW — alternates exploration and testing' },
  { name: 'gosm', desc: 'Goal-Oriented State Machine — routes input through the appropriate analysis chain' },
];

const tier3Categories = [
  {
    category: 'Research & Analysis',
    skills: ['lr', 'qr', 'dc', 'sta', 'exd', 'fia', 'cma', 'mr', 'src', 'sop', 'er', 'plr'],
  },
  {
    category: 'Writing & Communication',
    skills: ['w', 'pw', 'stl', 'pus', 'prd', 'cts', 'orm', 'fd', 'al'],
  },
  {
    category: 'Planning & Projects',
    skills: ['pji', 'de', 'pjc', 'op', 'pt', 'ria', 'dpl', 'ret', 'skm'],
  },
  {
    category: 'Business',
    skills: ['cd', 'mf', 'pos', 'neg', 'clr', 'fm', 'bm', 'ivs', 'roa'],
  },
  {
    category: 'Software',
    skills: ['cor', 'dbg', 'rf', 'dsn', 'ap', 'apid', 'ts', 'sep'],
  },
  {
    category: 'Career & Learning',
    skills: ['cpp', 'ip', 'rmo', 'sn', 'ska', 'dlp', 'spr', 'acr', 'lrs'],
  },
  {
    category: 'Decision Making',
    skills: ['dct', 'sel', 'crw', 'pwc', 'exv', 'mcd', 'rva', 'boc'],
  },
];
---

<Base title="Skills" activeNav="skills" description="221 structured reasoning skills for Claude Code, classified by universality.">
  <div class="page-header">
    <h1>Skills</h1>
    <p class="lead">221 structured reasoning skills for Claude Code. Each guides you through a specific type of thinking, classified by <a href="/about#universal-vs-heuristic">universality</a>.</p>
  </div>

  <div class="callout">
    <p><strong>Usage:</strong> type <code>/skillname your question</code> in a Claude Code session. For example: <code>/araw should I change careers</code></p>
  </div>

  <section>
    <h2><span class="tier-badge tier-exp">Experimental</span> ARAW, UAUA, GOSM</h2>
    <p>These use a different approach. They treat every claim as an unverified guess and test it by exploring what follows if it's right and what follows if it's wrong.</p>
    <div class="skill-grid">
      {experimental.map(s => (
        <a href={`/skills/${s.name}`} class="skill-item skill-item--link">
          <code>{s.name}</code>
          <div class="desc">{s.desc}</div>
        </a>
      ))}
    </div>
  </section>

  <section>
    <h2><span class="tier-badge tier-1">Tier 1</span> Start here</h2>
    <p>The most generally useful skills. They work on almost any problem and consistently produce non-obvious results.</p>
    <div class="skill-grid">
      {tier1.map(s => (
        <a href={`/skills/${s.name}`} class="skill-item skill-item--link">
          <code>{s.name}</code>
          <div class="desc">{s.desc}</div>
        </a>
      ))}
    </div>
  </section>

  <section>
    <h2><span class="tier-badge tier-2">Tier 2</span> Deep reasoning</h2>
    <p>Go deeper on specific types of thinking. Use when you need thoroughness on a particular kind of problem.</p>
    <div class="skill-grid">
      {tier2.map(s => (
        <a href={`/skills/${s.name}`} class="skill-item skill-item--link">
          <code>{s.name}</code>
          <div class="desc">{s.desc}</div>
        </a>
      ))}
    </div>
  </section>

  <section>
    <h2><span class="tier-badge tier-3">Tier 3</span> Specialized tools</h2>
    <p>These solve specific types of problems well. Less general but very effective in their domain.</p>
    {tier3Categories.map(cat => (
      <div class="category-section">
        <h3>{cat.category}</h3>
        <div class="skill-grid">
          {cat.skills.map(skillName => (
            <a href={`/skills/${skillName}`} class="skill-item skill-item--link">
              <code>{skillName}</code>
            </a>
          ))}
        </div>
      </div>
    ))}
  </section>

  <section>
    <h2><span class="tier-badge tier-4">Tier 4</span> Everything else</h2>
    <p>
      The remaining skills cover specific use cases: goal decomposition, procedure extraction, search methods, strategy discovery, self-auditing, and more.
      Browse <a href="https://github.com/benjam3n/reasoningtool/tree/main/claude-code-plugin/skills"><code>claude-code-plugin/skills/</code></a> for the complete list.
    </p>
  </section>
</Base>
